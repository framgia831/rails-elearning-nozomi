<div class="container-fluid main">
	<div class="row">
		<div class="col-md-1 center-block"></div>
		<div class="col-md-3 center-block">
			<div class="row">
				<div class="col-md-9 center-block">
            		<h2><strong>Dashboard</strong></h2>
            	</div>
            	<div class="col-md-2 center-block">
				</div>
			</div>
            <hr>
			<div class="row">
				<div class="col-md-5 center-block">
            	<%= image_tag current_user.image.url, id: :img_tumb2%>
            	</div>
				<div class="col-md-7">
            	<h3><strong><%= link_to current_user.name, user_url(current_user) %></strong></h3>
				<h5>@<%= current_user.department %></h5><hr>
				<h4 class="text-success"><i class="fas fa-award"></i><%= current_user.answers.count %> Words Learned</h4>
				<small class="text-muted pr-4 mb-5">in <%= current_user.lessons.count %> Categories</small>
            	<p class="pull-left"><%= link_to dashboard_path do%><i class="fa fa-arrow-circle-o-left"></i> Recent Activity<% end %></p>
				</div>
			</div>
		</div>

        <div class="col-md-1"></div>
		<div class="col-md-7">
				<h3 class="mt-2 pull-left"><i class="fa fa-book"></i><strong>Words You Learned</strong></h3>
					<div class="pagenate pull-right"><%= will_paginate @words %></div>		
						<table class="table table-striped table-responsive">
							<thead>
					    		<th>Words</th>
					    		<th>Answers</th>
					    		<th>Categories</th>
					    	</thead>
						<% @words.each do |word| %>
					    	<tbody>
			                    <td class="description">
			                        <h4><%= word.content %></h4>
			                    </td>
			                    <td class="description">
			                        <h4><%= word.choices.find_by(judge: true).content %></h4>
			                    </td>
			                    <td class="description">
			                        <h4><%= word.category.title %></h4>
			                    </td>
					    	</tbody>
						<% end %>
					</table>
				</div>
			</div>
		</div>
		<div class="col-md-1"></div>
		</div>
	</div>
</div>